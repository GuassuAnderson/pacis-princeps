"use client";
import {FormEvent,useState} from "react";
export function Newsletter(){const [saved,setSaved]=useState(false);function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=event.currentTarget;const email=new FormData(form).get("email")?.toString().trim().toLowerCase();if(email){const current=JSON.parse(localStorage.getItem("pp_newsletter_demo")||"[]") as string[];if(!current.includes(email))localStorage.setItem("pp_newsletter_demo",JSON.stringify([...current,email]));setSaved(true);form.reset();window.setTimeout(()=>setSaved(false),2200)}}return <form className="form-newsletter" onSubmit={submit}><input name="email" type="email" placeholder="Seu melhor e-mail" required/><button type="submit" className="btn btn-primario">{saved?"Salvo nesta demonstração ✓":"Quero receber"}</button></form>}
